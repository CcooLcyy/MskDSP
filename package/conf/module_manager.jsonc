{
  // ModuleManager 启动时自动加载的模块列表（建议仅填写 ConfigPusher）。
  // 其他模块建议通过 ConfigPusher 的配置进行按需启动。
  // 日志策略为内置配置（如需调整请修改 src/core/ModuleManager/Logger.cc）：
  // - 日志文件：./log/module_manager.log（追加写入）
  // - 每日 00:00 轮转，历史日志自动压缩为 .gz
  // - 仅保留最近 30 天
  "auto_start_modules": [
    "ConfigPusher"
  ]
}